# 2026-07-19

## Goal

继续实施 `build-swiftui-picacomic-mvp`，完成 Rasen task 4.1 的 Keychain token store。

## Changes

- 增加领域层 `TokenStore` 协议和可区分的存储错误。
- 增加基于 Security framework 的 `KeychainTokenStore`。
- token 使用 `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`，不允许迁移到其他设备。
- 保存操作优先覆盖已有条目，不存在时创建；删除操作对不存在条目幂等。
- 增加 stub 行为测试与真实系统 Keychain 往返测试。

## Verification

- 完整离线测试集：26 项通过，2 项 credential-gated live test 按设计跳过。
- Keychain 专项测试：9 项在 iPhone Air / iOS 27 上通过，包含真实 Security API 保存、恢复、覆盖和删除。
- `git diff --check`：通过。

## Decisions

- 领域层只依赖 `TokenStore`，不直接依赖 Security framework，后续 AccountRepository 可注入测试替身。
- Keychain 不可用、存储数据损坏和调用方提供空 token 使用不同错误，避免把安全存储故障误报为未登录。

## Next

- 实现 Rasen task 4.2：AccountRepository 与 restoring、unauthenticated、authenticating、authenticated、failed 状态。

---

## Goal

完成 Rasen task 4.2 的账号仓库和显式会话状态机。

## Changes

- 增加 `AccountSessionState` 五种状态和结构化失败信息。
- 增加 actor 隔离的 `DefaultAccountRepository`，集中管理内存 token、会话恢复和登录状态转换。
- 增加 `APIAccountAuthenticator`，把登录 endpoint 与领域仓库隔离。
- 登录成功时先写入 Keychain，写入失败不得进入 authenticated 或保留内存 token。
- 仓库层阻止认证中的重复提交，失败状态只保留规范化邮箱，不保留密码。

## Verification

- AccountRepository 专项测试：7 项在 iPhone Air / iOS 27 上通过。
- 测试使用 `EXCLUDED_SOURCE_FILE_NAMES=icon.heic` 临时排除用户并行产生的失效资源引用；未修改该设计素材或工程引用。

## Problems

- `PicaHub.xcodeproj` 当前由用户侧改动引用根目录 `icon.heic`，但实际素材位于 `docs/design/icon.heic`，导致常规构建失败。该问题与账号功能无关，且未纳入本提交。

## Next

- 实现 Rasen task 4.3 的登录 Feature Model 和 SwiftUI 登录页。
- 在图标功能所属提交中修正 `icon.heic` 工程路径，使常规构建恢复。

---

## Goal

完成 Rasen task 4.3 的 SwiftUI 登录页面与交互模型。

## Changes

- 增加 `LoginModel`，负责输入规范化、本地校验、提交进度、错误展示和认证结果映射。
- 增加紫色渐变、半透明卡片风格的 `LoginView`，保持 iOS 17 兼容。
- 密码在提交开始时立即从可观察 UI 状态清空；失败后仅保留邮箱。
- UI 和 AccountRepository 双层阻止重复登录请求。
- 增加 accessibility identifiers，为后续 UI 自动化测试保留稳定入口。

## Verification

- LoginModel 专项测试：4 项在 iPhone Air / iOS 27 上通过。
- 登录页面随测试 target 完成 Swift 编译。
- 因用户侧 `icon.heic` 失效引用，仍使用临时排除构建参数；未修改该图标改动。

## Next

- 实现 task 4.4：应用启动依赖装配、会话恢复和根路由。

---

## Goal

完成 Rasen task 4.4 的启动会话恢复与应用根路由。

## Changes

- 增加 `AppDependencies`，统一创建生产 Keychain store、登录 APIClient 和 AccountRepository。
- 增加 `AppRootModel` 与 `AppRootView`，根据会话状态选择恢复、登录、已认证或恢复失败页面。
- 启动期间固定显示不可交互 restoring 页面，避免登录页或受保护内容闪现。
- 登录成功后同步仓库状态并切换根路由。
- 将应用入口从 Debug 协议 Spike 切换到正式账号根路由；Spike 源码保留用于诊断。

## Verification

- AppRootModel 专项测试：5 项在 iPhone Air / iOS 27 上通过。
- 不带临时排除参数的完整测试集：43 项通过，2 项 credential-gated live test 按设计跳过。
- 新 Debug 构建已安装并启动到连接的 iPhone。
- 系统 Keychain 往返能力已在 task 4.1 真机验证；端到端 UI 重启场景留在 task 4.6 自动化覆盖。

## Problems

- 验证前半段曾受用户侧根目录 `icon.heic` 失效引用影响；该工程引用随后已恢复，最终完整测试使用正常构建参数通过。

## Next

- 实现 task 4.5：集中 logout、401 invalidation 和认证请求取消。

---

## Goal

根据 `docs/design/icon.heic` 的三外观设计替换 iOS AppIcon。

## Changes

- 从合成交付图恢复无预裁圆角的方形图标母图。
- 生成构图一致的 Any/Light、Dark 和 Tinted 三种外观。
- 将三张 PNG 映射到 `AppIcon.appiconset/Contents.json` 的对应槽位。

## Verification

- 三张母图均转换为 1024×1024、RGB、无 alpha 的 PNG。
- 无签名通用 iOS 构建通过，`actool` 成功编译并链接 AppIcon 资源，无图标警告。

## Decisions

- 源 HEIC 是已合成的圆角预览板，无法通过简单裁切获得合规的方形 AppIcon；因此按预览恢复被圆角遮挡的边角，避免将黑色角落烘焙到图标内。

## Next

- 在真机主屏分别切换浅色、深色和着色模式，确认系统显示效果。

---

## Goal

完成 Rasen task 4.5 的集中注销、HTTP 401 失效和认证请求取消。

## Changes

- 增加 `AuthenticatedRequestController`，登记并集中取消所有带 token 的活动 transport task。
- APIClient 的认证请求接入共享控制器；登录等无认证请求不受影响。
- AccountRepository 增加 logout、幂等 invalidate 和持续 `AsyncStream` 状态更新。
- logout/401 先清内存 token 并发布未登录状态，再取消请求和删除 Keychain。
- AppRoot 持续订阅状态流，401 无需页面轮询即可自动返回登录。
- 已认证占位页增加带确认的退出登录操作。

## Verification

- 完整测试初轮：46 项通过，2 项 live test 按设计跳过。
- 修复状态流协议 witness 后，AccountRepository/APIClient 20 项专项测试通过。
- AppRoot 6 项专项测试通过，包含会话失效自动返回登录。

## Problems

- 新增测试首次发现 actor 的同步 `sessionStateUpdates()` 没有成为 async 协议 witness，调用落入一次性默认流；改为显式 async witness 后持续状态推送通过。

## Next

- 完成 task 4.6：账号会话单元测试补全、UI 自动化和真实登录重启验证。

---

## Goal

完成 Rasen task 4.6 的账号会话自动化验收。

## Changes

- 增加仅在 Debug 且显式传入 `--uitesting` 时启用的确定性账号仓库。
- UI 测试覆盖登录成功、服务拒绝、密码清空、退出登录和已认证状态重启恢复。
- AppRoot 忽略登录流程内部的 authenticating 和带邮箱 failed 状态，让进度与错误留在 LoginView 展示。
- UI 测试不访问真实 API、真实账号或生产 Keychain。

## Verification

- 账号 UI 自动化：3 项在 iPhone Air / iOS 27 上通过。
- 单元测试基线：48 项通过，2 项 credential-gated live test 按设计跳过。
- UI 用例与单元测试合计覆盖登录成功、拒绝、重启恢复、logout 和重复 401。

## Problems

- 第一轮 UI 测试错误限定 accessibility 元素类型，并暴露登录失败被根路由误判为恢复失败的问题。
- 第二轮发现 SecureField 清空后 UIAutomation 返回 nil，且真机键盘偶发漏录最后一个字符；改为由单元测试验证密码值清空，UI 测试验证安全输入框和错误状态，Debug stub 只以固定测试密码判断成功。

## Next

- 账号会话 tasks 4.1 至 4.6 全部完成，进入 task 5.1 分类仓库和分类页面。

---

## Goal

完成 Rasen task 5.1 的分类仓库和分类页面。

## Changes

- 增加 `CategoryRepository` 与 `APICategoryRepository`，保留服务端顺序并过滤 `isWeb == true` 的网页分类。
- 增加 CategoryModel，明确处理 loading、content、empty、error、retry 和 refresh；刷新期间保留旧内容，失败时继续展示旧内容并提示原因。
- 增加自适应分类网格、图片占位和可选描述 fallback，并把分类页接入已认证根路由。
- Debug UI 测试注入本地分类仓库，不访问真实服务。

## Verification

- 分类专项测试：6 项在 iPhone Air / iOS 27 上通过。
- 完整单元测试：54 项通过，2 项 credential-gated live test 按设计跳过。
- 已认证重启 UI 用例中的分类内容断言通过；开发阶段不要求每个 task 重跑完整账号 UI 回归。

## Problems

- 完整 UI 回归中的登录输入仍可能受真机 UIAutomation 偶发漏录影响；该问题与分类页面逻辑无关，留待阶段验收处理。

## Next

- 进入 task 5.2：分类漫画分页浏览、排序、去重与有界下一页加载。

---

## Goal

优化分类动态封面，使其首次加载后保持不变，直到用户手动下拉刷新。

## Changes

- `APICategoryRepository` 增加会话内快照，普通加载复用首次成功分类数据，手动刷新才请求替换。
- 增加分类专用图片缓存，按分类 ID 固定首次成功图片，避免 SwiftUI 重绘、滚动复用和页面重建触发重复图片请求。
- 手动刷新成功后清除图片缓存，并使用 `reloadIgnoringLocalCacheData` 绕过 URLCache；刷新失败保留旧快照和旧图片。
- 以专项测试覆盖分类快照复用、显式刷新、图片 URL 变化时保持首图，以及手动刷新绕过 URLCache。

## Verification

- 分类缓存专项测试：8 项在 iPhone Air / iOS 27 上通过。
- 未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Next

- 继续 task 5.2 分类漫画分页浏览。

---

## Goal

完成 Rasen task 5.2 的分类漫画分页浏览。

## Changes

- 增加 `ComicRepository` 与 API 适配器，使用分类标题、排序和页码请求漫画列表。
- 增加 `ComicBrowseModel`，覆盖首屏、空态、错误、刷新、排序切换、下一页加载和下一页局部重试。
- 下一页请求同一时刻最多一个；只在最后条目出现且服务端仍有后续页时触发，合并时按漫画 ID 去重。
- 分类卡片接入漫画浏览导航；当前使用轻量标题/作者行，完整封面与元数据展示留给 task 5.3。

## Verification

- 漫画分页专项测试：7 项在 iPhone Air / iOS 27 上通过。
- 分类进入漫画分页页面 UI 测试：1 项在真机通过。
- 未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Next

- 进入 task 5.3：漫画列表封面、标题、作者、元数据 fallback、图片占位和图片重试。

---

## Goal

完成 Rasen task 5.3 的漫画列表单元格和封面重试。

## Changes

- 漫画行升级为封面加摘要卡片，展示标题、作者、最多三个分类、页数、章节、连载状态、喜欢数和观看数。
- 增加纯 presentation 映射，缺失作者与摘要元数据时使用明确 fallback，重复分类标签自动去重。
- 增加独立封面状态模型；加载失败只显示单图重试，不影响列表、排序和分页。
- 封面重试使用 `reloadIgnoringLocalCacheData`，避免持续命中损坏或过期的 URLCache 响应。

## Verification

- 元数据映射与封面重试专项测试：4 项在 iPhone Air / iOS 27 上通过。
- 分类进入漫画列表卡片 UI 测试：1 项在真机通过。
- 未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Problems

- 首次 UI 断言尝试读取封面容器 identifier，但加载态内部元素接管了 accessibility；改为验证漫画卡片和作者文本，不修改视觉层级。

## Next

- 进入 task 5.4：可取消分页搜索、空关键词校验和无结果状态。

---

## Goal

完成 Rasen task 5.4 的可取消分页搜索。

## Changes

- 漫画仓库增加 advanced-search 请求，使用独立 query 编码生成 `page` 参数，并发送固定 newest sort 和空分类数组。
- 发现页增加搜索入口和搜索页面，复用漫画卡片并支持分页、去重、刷新及下一页局部重试。
- 空白关键词在本地显示校验提示，不调用仓库；有效关键词无结果时显示专用 no-results 状态。
- 搜索模型持有活动请求 Task；新关键词、刷新和离开页面会取消旧请求，请求代次进一步阻止过期结果写回。

## Verification

- 搜索 endpoint 与状态模型专项测试：6 项在 iPhone Air / iOS 27 上通过。
- 发现页进入搜索并展示结果 UI 测试：1 项在真机通过。
- 未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Problems

- 首次编译发现 Debug 搜索仓库的辅助方法与 `page` 参数同名；重命名后通过。
- 测试辅助方法受 MainActor 推断影响；标记为 `nonisolated` 后，取消旧请求测试通过。

## Next

- 进入 task 5.5：漫画详情与章节的独立加载、局部失败和重试。

---

## Goal

完成 Rasen task 5.5 的漫画详情与章节独立加载。

## Changes

- 增加详情仓库协议与 API 适配器，分别读取漫画详情和指定章节页。
- `ComicDetailsModel` 并发加载详情与章节第一页，两条资源链路拥有独立 idle/loading/loaded/failed 状态。
- 详情页展示封面、标题、作者、简介、标签、页数、章节、连载、喜欢、观看、评论和收藏状态。
- 详情或章节任一失败时保留另一部分内容，并提供只重试失败资源的操作；空章节显示独立空态。
- 分类列表和搜索结果的漫画导航链接接入详情页面，并增加稳定可交互 accessibility identifier。

## Verification

- 详情仓库与独立状态专项测试：4 项在 iPhone Air / iOS 27 上通过。
- 漫画列表进入详情并展示章节 UI 测试：1 项在真机通过。
- 未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Problems

- 初始 UI 测试点击了卡片内部展示元素，未触发外层 NavigationLink；为真实导航链接增加稳定标识后通过。

## Next

- 进入 task 5.6：加载全部章节页、去重并确定服务端章节顺序。

---

## Goal

完成 Rasen task 5.6 的完整多页章节读取、去重和顺序归一化。

## Changes

- `ComicDetailsRepository` 改为直接提供完整章节数组，分页细节收口到 API 仓库。
- API 仓库先读取第一页报告的总页数，再按页码合并全部页面，并按章节 ID 保留首次出现项。
- 依据 HaKa 参考实现验证的协议行为，将服务端合并结果整体反转为最新章节在前。
- 详情模型和 UI 测试仓库同步使用完整章节接口。
- 增加三页响应、跨页重复章节和最终顺序的仓库测试。

## Verification

- 章节仓库与详情模型专项测试：4 项在 iPhone Air / iOS 27 上通过。
- 未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Next

- 进入 task 5.7：补齐发现模块分页、刷新保留、取消、空数据、局部失败和可选字段测试。

---

## Goal

完成 Rasen task 5.7 的发现模块边界场景综合验收。

## Changes

- 汇总并执行分类、漫画浏览、搜索、详情、封面、元数据和 API 仓库测试，确认分页、刷新保留、取消、空数据、局部失败与缺少可选字段均有自动化覆盖。
- 执行分类进入漫画列表、发现页搜索和漫画详情三条 UI 导航链路，确认发现模块主要入口保持可用。
- 本项没有重复增加与既有专项测试等价的新测试代码；以连续通过的 34 项单元测试和 3 项 UI 测试作为发现阶段验收证据。

## Verification

- 发现模块专项单元测试：34 项在 iPhone Air / iOS 27 上通过。
- 发现模块 UI 测试：3 项在 iPhone Air / iOS 27 上通过。
- 未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Next

- 进入 task 6.1：章节图片完整分页读取、有界并发、确定顺序、取消和去重。

---

## Goal

完成 Rasen task 6.1 的章节图片完整分页读取。

## Changes

- 增加 `ChapterImageRepository` 和 API 适配器，把章节图片分页细节与后续阅读器隔离。
- 先读取第一页总页数，再以默认最多 3 个并发请求的滑动窗口读取剩余页面。
- 不依赖响应完成时序，始终按页码和页面内顺序重组，并按图片 ID 保留首次出现项。
- 顶层 Task 取消时由 throwing task group 取消全部在途分页请求。

## Verification

- 章节图片仓库专项测试：2 项在 iPhone Air / iOS 27 上通过。
- 覆盖乱序完成、有界并发、跨页重复 ID、空页面和整体取消。
- 未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Next

- 进入 task 6.2：建立 URLCache 原图缓存、有成本限制的解码图片缓存和单图重试。

---

## Goal

完成 Rasen task 6.2 的通用两级图片管线。

## Changes

- 增加 MainActor 图片管线，原始响应沿用 URLSession/URLCache，解码图片存入带总成本上限的 `NSCache`。
- 同一 URL 的并发读取共享一个在途 Task，避免滚动复用时重复下载和解码。
- 单图重试先移除对应解码图片，再用 `reloadIgnoringLocalCacheData` 获取原图。
- 提供单图取消、全部在途任务取消和只清除解码缓存三个生命周期操作。

## Verification

- 图片管线专项测试：4 项在 iPhone Air / iOS 27 上通过。
- 覆盖解码缓存命中、同 URL 请求去重、重试绕过 URLCache 和清理解码缓存后的原图缓存策略。
- 未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Next

- 进入 task 6.3：竖向连续滚动阅读器、可见图片优先和有界前向预取。

---

## Goal

完成 Rasen task 6.3 的竖向连续阅读、可见图片优先与有界前向预取。

## Changes

- 增加独立阅读图片调度器，默认优先加载当前可见项、向后预取 2 项，并限制最多 3 个同时加载任务。
- 使用滚动区域内图片的实际几何位置确定当前项，避免把 `LazyVStack` 的提前构建当成已阅读位置。
- 可见位置改变和阅读器离开时取消过时图片任务；内存警告时只清理解码缓存。
- 增加连续竖向图片视图和单图失败重试入口。

## Verification

- 阅读图片调度专项测试：2 项在 iPhone Air / iOS 27 上通过。
- 覆盖可见项优先、前向预取边界、最大并发和位置变化取消。
- `git diff --check` 通过；未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Next

- 进入 task 6.4：上一章/下一章方向、边界处理与旧任务取消。

---

## Goal

完成 Rasen task 6.4 的上一章/下一章导航、边界和旧任务取消。

## Changes

- 在最新章节在前的统一列表上明确叙事方向：上一章走向更早章节，下一章走向更新章节。
- 最新章和最早章分别暴露不可用方向，供后续阅读器按钮直接禁用。
- 章节切换取消旧章节请求和旧图片工作，并清空旧图片状态。
- 使用加载代次和章节 ID 阻止不响应取消的迟到结果覆盖当前章节。

## Verification

- 章节导航专项测试：3 项在 iPhone Air / iOS 27 上通过。
- 覆盖方向、两端边界、切换取消和迟到结果隔离。
- 首次重跑因前一轮被中断的真机测试进程未及时退出而等待 runner；进程退出后同一测试集完整通过。
- `git diff --check` 通过；未执行完整回归测试，符合当前开发阶段的按功能验证约定。

## Next

- 进入 task 6.5：按漫画保存阅读进度，以及陈旧章节和图片索引回退。
