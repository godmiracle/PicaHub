# 2026-07-18

## Goal

为 PicaHub 建立仓库根目录 `.gitignore`，排除外部参考项目、本地文件和开发工具产物。

## Changes

- 忽略根目录下的 `haka_comic-main/`、`pikapika/` 和 `1.md`。
- 忽略 macOS 元数据、IDE 用户设置、Xcode 用户数据与构建产物、SwiftPM 构建状态。
- 忽略本地 `.env` 变体，但保留 `.env.example` 供 Git 追踪。
- 同步更新 `docs/todo.md` 的仓库维护任务。

## Problems

- 首次验证脚本误用 zsh 特殊变量 `path`，临时覆盖了 `PATH`；修正变量名后重新执行并通过验证。

## Decisions

- `rasen/` 属于项目规格与变更记录，不加入忽略。
- 不忽略 `Package.resolved`、源码、测试和项目文档。

## Next

- 无。

---

## Goal

开始实施 Rasen change `build-swiftui-picacomic-mvp`，完成 SwiftUI 工程确认、协议客户端基础、离线测试和真实协议 Spike 入口。

## Changes

- 确认目标 iPhone 运行 iOS 27，并将 deployment target 从 iOS 27 调整为 iOS 17。
- 建立 Domain、Infrastructure、Feature 和 Shared 源码边界。
- 实现 API environment、确定性 query 编码、HMAC-SHA256 签名、endpoint、逐请求 headers、URLSession transport、响应 envelope 和错误映射。
- 实现登录、分类、漫画、详情、章节、图片和收藏基础 DTO 与 endpoint。
- 实现图片 URL `/static/` 与代理路线规则。
- 增加固定参考签名、请求、响应、错误、重试、模型和图片 URL 测试。
- 增加环境变量 live test 和 Debug-only 真机协议 Spike 界面；密码不落盘，token 只存内存。
- 将 Debug 构建安装并启动到已连接 iPhone。

## Verification

- 通用 iOS Debug build：通过。
- 测试 target build-for-testing：通过。
- iPhone 单元测试：15 项通过。
- Live protocol tests：2 项因未提供凭据按设计跳过。
- `git diff --check`：通过。

## Problems

- 沙箱内 CoreSimulatorService 不可用；改用沙箱外通用 device build 和已连接 iPhone 测试。
- 初次运行整个 scheme 测试时 UI 测试 result bundle 不完整；缩小为 `PicaHubTests` 后成功完成。
- 当前没有真实账号环境变量，协议门禁尚未完成。

## Decisions

- 真实协议门禁通过前不实现账号、发现、阅读和收藏产品 UI。
- Debug Spike 使用临时内存凭据，并要求收藏 mutation 二次确认和恢复原状态。
- 写请求不做自动网络重试，防止收藏等操作重复执行。

## Next

- 用户在已安装的 Debug Spike 中运行只读验证。
- 只读通过后，经用户确认运行收藏写入和恢复验证。
- 记录 live host、设备、日期和协议偏差，完成 Rasen tasks 3.2 至 3.8。

---

## Goal

定位 Debug Spike 的响应解码失败，并使用用户临时提供的账号完成真实协议门禁。

## Changes

- 将分类 `_id` 调整为可选远端标识，并在缺失时使用基于标题的稳定本地标识。
- 增加不包含响应正文的安全解码诊断，显示失败阶段、字段路径和期望类型。
- 增加分类缺少 `_id`、断网映射和取消映射测试。
- 通过临时 `.xctestrun` 配置向真机测试进程注入凭据，测试结束自动删除配置。
- 更新 Rasen tasks 3.2 至 3.8、项目上下文和 TODO 状态。

## Verification

- iPhone 只读真实协议链路：通过（登录、分类、列表、详情、章节分页、图片分页和图片下载）。
- iPhone 可逆收藏验证：通过（写入、收藏列表回读、恢复原状态）。
- iPhone 离线单元测试：18 项通过；未提供凭据的常规运行中 2 项 live test 按设计跳过。
- 凭据未写入项目，临时 `.xctestrun` 文件由退出清理器删除。

## Problems

- 普通 shell 环境变量不会自动传递到真机 XCTest 进程；改用构建产物同目录下的临时 `.xctestrun` 配置。
- 临时配置首次放在 `/private/tmp` 根目录，导致 `__TESTROOT__` 解析错误；移回构建产物目录后通过。

## Decisions

- 协议 Spike 门禁已经满足，可以开始账号会话和产品 Feature UI。
- 服务端分类 `_id` 按可选字段处理；缺失时不得阻断整个分类响应。
- Debug 解码错误只展示结构路径，不记录原始响应或敏感值。

## Next

- 从 Rasen task 4.1 开始实现 Keychain token store 和账号会话。
